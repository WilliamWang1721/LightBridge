# 0.3.8 版本更新

## 修复
1. 控制仪表台组件不可见
    - React 控制台页面不再挂在 `display: contents` 根节点上，避免 WebKit 把侧栏、顶栏和仪表盘卡片从盒模型中丢掉。
    - 仪表盘卡片改用明确的 HSL 语义色，并在现代主题层强制卡片底色与文字色，防止组件与页面背景融为一体。

## 升级说明
1. 本版本无需数据库或配置迁移
    - 升级二进制或容器并重启服务即可生效。
    - 前端为界面修复，请同时升级前后端。
